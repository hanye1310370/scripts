

 show crete database web_common ;
 CREATE DATABASE `web_common` /*!40100 DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci */
 
  CREATE DATABASE `web_common` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci；

create table travelrecord (id bigint not null primary key,user_id varchar(100),traveldate DATE, fee decimal,days int);




本机环境：
http://qa1-member.fx.com/?lid=910

正式环境：
http://zz.1fangxin.net/?zlid=910




DB_CONNECTION=mysql
DB_HOST=
DB_PORT=3306
DB_DATABASE=
DB_USERNAME
DB_PASSWORD=


删除文件中含有某个字符串的行：
sed -i '/-/d' youfilename.log
sed -i  '/Table structure/d' zz-fx-com_-_副本.sql |grep -i "Table structure"
删除文件中含特定字符串的行[bash]:
 
sed -e '/abc/d'  a.txt   // 删除a.txt中含"abc"的行，但不改变a.txt文件本身，操作之后的结果在终端显示
 
sed -e '/abc/d'  a.txt  > a.log   // 删除a.txt中含"abc"的行，将操作之后的结果保存到a.log
 
sed '/abc/d;/efg/d' a.txt > a.log    // 删除含字符串"abc"或“efg"的行，将结果保存到a.log
 
其中，"abc"也可以用正则表达式来代替。
 


[root@centos dnmp]# cat /etc/docker/daemon.json 
{
"registry-mirrors": ["https://pf16ukja.mirror.aliyuncs.com"]
}


测试：
101.201.73.179

ssh -i ./.ssh/id_rsa erp-fx-com@101.201.73.179
